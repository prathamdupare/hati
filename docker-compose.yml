services:
  hati:
    build: .
    image: hati-dashboard:latest 
    container_name: hati
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./hati.yaml:/app/hati.yaml:ro
    environment:
      - TZ=Asia/Kolkata
